[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "python -m pip install -e ."
]

[start]
cmd = "uvicorn whatsapp_langchain.server.main:app --host 0.0.0.0 --port $PORT"
