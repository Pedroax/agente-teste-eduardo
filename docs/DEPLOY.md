# Deploy no Railway

> Esta seção será implementada após a construção da API e Worker (Fase 2+).

## O que será coberto

- Deploy de 4 serviços no Railway (API, Worker, Frontend, PostgreSQL)
- Configuração do Twilio para receber mensagens WhatsApp
- Variáveis de ambiente para produção
- Auto-deploy via GitHub

Enquanto isso, use `make dev` para desenvolver e testar agentes localmente via LangGraph Studio.

Voltar para o [README](../README.md).
